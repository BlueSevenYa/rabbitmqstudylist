<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.shiqi.mapper.BrokerMessageLogMapper" >
    <resultMap id="BaseResultMap" type="com.shiqi.entity.BrokerMessageLog" >
        <id column="id" property="id" jdbcType="VARCHAR" />
        <result column="message" property="message" jdbcType="VARCHAR" />
        <result column="try_count" property="tryCount" jdbcType="INTEGER" />
        <result column="status" property="status" jdbcType="VARCHAR" />
        <result column="next_Retry" property="nextRetry" jdbcType="java.sql.Timestamp" />
        <result column="create_time" property="createTime" jdbcType="java.sql.Timestamp" />
        <result column="update_time" property="updateTime" jdbcType="java.sql.Timestamp" />

    </resultMap>
    <sql id="Base_Column_List" >
        id, message, try_count,status,next_Retry,create_time,update_time
    </sql>

    <insert id="insertBML" parameterType="com.shiqi.entity.BrokerMessageLog" >
        insert into broker_message_log (id, message, try_count,status,next_Retry,create_time,update_time)
        values (#{id},#{message},#{try_count},#{status},#{next_Retry},#{create_time},#{update_time})
    </insert>

    <update id="changeBrokerMessageLogStatus">
        update broker_message_log bml
        set bml.status=#{status,jdbcType=VARCHAR},
        bml.update_time=#{updateTime,jdbcType=TIMESTAMP}
        where bml.id=#{messageId,jdbcType=VARCHAR}
    </update>


    <select id="query4StatusAndTimeoutMessage" resultMap="BaseResultMap">
        <![CDATA[
          select id, message, try_count, status, next_retry, create_time, update_time
              from broker_message_log bml
              where status = '0'
              and next_retry <= sysdate()
          ]]>
    </select>

    <update id="update4ReSend" >
        update broker_message_log bml
        set bml.try_count = bml.try_count + 1,
        bml.update_time = #{updateTime, jdbcType=TIMESTAMP}
        where bml.id = #{messageId,jdbcType=VARCHAR}
    </update>



</mapper>